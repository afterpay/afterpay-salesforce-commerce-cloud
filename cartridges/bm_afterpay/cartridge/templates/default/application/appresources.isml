<iscontent type="text/javascript" charset="UTF-8" compact="true"/>

(function(){
    window.Resources = <isprint value="${JSON.stringify(pdict.ResourceHelper.getResources())}"/>;
    window.Urls = <isprint value="${JSON.stringify(pdict.ResourceHelper.getUrls())}"/>;
}());
